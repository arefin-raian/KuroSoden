"""Mapping-editor web package (Telegram Mini App).

A tiny FastAPI app + a single-page editor that lets staff re-order franchise
files, assign seasons, and exclude non-episodes visually — replacing the ``3 S2``
text grammar and the read-only Telegraph guide for BOTH the DDL and torrent
mapping flows. Served by uvicorn alongside the bots; opened via a WebApp button.
"""
