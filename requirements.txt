--index-url https://pypi.org/simple
# NOTE: rar/7z extraction for the DDL source needs the 7-Zip *binary* (there is no
# reliable pip package). The launchers (run.sh / run.bat) auto-install it; .zip
# archives need no external tooling. See nekofetch/sources/_archive.py.
pyrofork>=2.3.0
sqlalchemy[asyncio]>=2.0.30
asyncpg>=0.29.0
alembic>=1.13.0
motor>=3.4.0
redis>=5.0.0
apscheduler>=3.10.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
pyyaml>=6.0
structlog>=24.1.0
cryptography>=42.0.0
httpx>=0.27.0
curl-cffi>=0.7.0
yt-dlp>=2026.6.0
aiofiles>=23.2.0
colorama>=0.4.6
TgCryptoX>=1.0.1
uvloop>=0.19.0 ; sys_platform != "win32"
rich>=13.0.0
playwright>=1.44.0
pillow>=10.0.0
beautifulsoup4>=4.12.0
# Pure-Python language detection for untitled/untagged subtitle tracks — names a
# track by its cue CONTENT (a deterministic Unicode-script pass covers CJK/Hindi/
# Arabic/…; langdetect handles Latin scripts) instead of the @AniXWeebs placeholder.
langdetect>=1.0.9

